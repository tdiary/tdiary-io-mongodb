language: ruby
cache: bundler

rvm:
  - 2.3.8
  - 2.4.5
  - 2.5.3
  - 2.6.0
  - ruby-head

matrix:
  allow_failures:
    - rvm: ruby-head

before_install:

script: bundle exec rake spec

services:
  - mongodb
